# Frozen candidate FZ-2026-08-23-4132711F

## Candidate identity

The post-B3 release candidate is frozen at:

- Git commit: `4132711f9d0ad74ff41b26deff7b9966d6e54e94`
- Candidate ref: `candidate/frozen-4132711f`
- Git tree: `972a8bc2ca1b15262b80ed77712676c636445475`
- `sef.py` SHA-256: `bfeda790cd70c78ceb9fae862441df35c9bfe819001c0d429c09c0dde2a0c2bd`
- Runtime version lineage: `1.4`

The candidate ref points to the exact B3-merged repository commit. No freeze metadata commit is part of the candidate itself.

## Qualification inherited from B3

B3 aggregate evidence was PASS:

- DEV: 38/38 PASS
- B1: 10/10 PASS
- B2: 10/10 PASS
- calibrated RC-8: 14/14 PASS
- first CHALLENGE replay: 10/10 PASS as `CONSUMED_REGRESSION_ONLY`
- blockers: none
- `freeze_ready=true`

B3 evidence:

- workflow run: `32646778644`
- artifact: `9495082319`
- digest: `sha256:c49fca7cabdb10881bf9e2e563b684acee9def2c0adc788ad783deceb18e047c`

## Freeze rule

From this point, no runtime tuning is permitted for the candidate used by CHALLENGE v2.

If the frozen candidate is changed, even for a seemingly small behavioral fix, the changed runtime is a different candidate and requires a new freeze before an independent holdout claim can be made.

Repository documentation, evaluator material and evidence records may evolve outside the frozen candidate commit, but CHALLENGE v2 must execute the exact runtime identified above.

## Holdout separation

At freeze time, CHALLENGE v2 is not materialized in the repository and has not been used for tuning.

After this freeze is validated, a fresh evaluator branch may materialize CHALLENGE v2. Its scenarios must be genuinely new relative to DEV, B1, B2, RC-8 and the consumed first CHALLENGE.

The first valid CHALLENGE v2 harness run is the independent candidate verdict. A harness error invalidates the measurement; a legitimate benchmark failure does not.

After the first valid run, CHALLENGE v2 becomes consumed for future tuning.

## Release lineage note

The frozen runtime still declares `VERSION="1.4"`. Therefore the existing release-attestation workflow is compatible with a `v1.4.x...` release tag. Moving to a `v1.5.x` runtime would require changing the frozen runtime identity and requalifying that new candidate.

## L2 status

- Codex real-agent L2: NOT_RUN
- Claude real-agent L2: NOT_RUN

L2 is intentionally after the fresh deterministic holdout in the current finish plan.
