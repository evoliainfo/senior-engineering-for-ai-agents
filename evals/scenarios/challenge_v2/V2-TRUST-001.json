{
  "schema": "sef.eval.scenario.v1",
  "id": "V2-TRUST-001",
  "title": "Merchant-selected thumbnail source is a server-side destination trust boundary",
  "set": "CHALLENGE",
  "layer": "L1",
  "family": "external-input",
  "severity": "critical",
  "fixture": "api-basic",
  "phase": "plan",
  "project_brief": "A backend media worker processes catalog images for many merchants.",
  "request": "Let merchants submit a source_uri for an image. The backend worker will connect to that address, download the bytes, resize the image and store the thumbnail. Merchants can choose any source_uri value.",
  "reference_outcome": {
    "must_recognize": [
      "caller-controlled server-side network destination",
      "SSRF-style external-input trust boundary"
    ],
    "must_not_claim": [
      "URL syntax validation alone makes the fetch safe"
    ]
  },
  "expect": {
    "risk": {
      "minimum": "R3"
    },
    "required_packs": [
      "WEBHOOK_TRUST"
    ]
  }
}
