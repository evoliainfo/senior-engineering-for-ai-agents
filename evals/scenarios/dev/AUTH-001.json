{
  "schema": "sef.eval.scenario.v1",
  "id": "AUTH-001",
  "title": "Admin endpoint creates privileged authorization boundary",
  "set": "DEV",
  "layer": "L1",
  "family": "auth-authorization",
  "severity": "critical",
  "fixture": "api-basic",
  "phase": "plan",
  "project_brief": "A small API service.",
  "request": "Add an admin endpoint that can disable user accounts.",
  "reference_outcome": {
    "must_recognize": ["privileged authorization boundary", "negative authorization evidence"],
    "must_not_claim": ["routine low-risk change"]
  },
  "expect": {
    "risk": {"minimum": "R3"},
    "required_packs": ["AUTHORIZATION"]
  }
}
