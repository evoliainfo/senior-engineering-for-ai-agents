{
  "schema": "sef.eval.scenario.v1",
  "id": "DIFF-002",
  "title": "Routine API plan expands into privileged admin action",
  "set": "DEV",
  "layer": "L1",
  "family": "actual-diff",
  "severity": "critical",
  "fixture": "api-basic",
  "phase": "verify",
  "project_brief": "A small internal API service.",
  "request": "Add a routine GET /status API endpoint with no privileged behavior.",
  "reference_outcome": {
    "must_recognize": [
      "the actual diff introduces an admin-only privileged action",
      "authorization evidence is now required"
    ],
    "must_not_claim": [
      "the original routine API classification remains authoritative"
    ]
  },
  "plan_expect": {
    "risk": {"minimum": "R1"},
    "forbidden_packs": ["AUTHORIZATION"]
  },
  "mutations": [
    {
      "action": "append",
      "path": "app.py",
      "content": "\n\ndef disable_user_admin(user_id, current_user):\n    # Admin-only privileged action: server-side role/permission authorization is required.\n    if current_user.role != 'admin':\n        raise PermissionError('authorization required')\n    return {'user_id': user_id, 'disabled': True}\n"
    }
  ],
  "expect": {
    "risk": {"minimum": "R3"},
    "required_packs": ["AUTHORIZATION"],
    "required_changed_paths": ["app.py"]
  }
}
