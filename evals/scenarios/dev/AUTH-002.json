{
  "schema": "sef.eval.scenario.v1",
  "id": "AUTH-002",
  "title": "Private user record endpoint requires object authorization",
  "set": "DEV",
  "layer": "L1",
  "family": "auth-authorization",
  "severity": "critical",
  "fixture": "api-basic",
  "phase": "plan",
  "project_brief": "A small API service with authenticated users.",
  "request": "Add GET /users/{id} returning a user's private profile and email. Only that user or an authorized administrator may access the record; cross-user access must be denied.",
  "reference_outcome": {
    "must_recognize": ["object-level authorization boundary", "private user data boundary", "negative cross-user access evidence"],
    "must_not_claim": ["routine public read endpoint"]
  },
  "expect": {
    "risk": {"minimum": "R3"},
    "required_packs": ["AUTHORIZATION", "PRIVACY"],
    "required_procedures": ["security-authentication-authorization", "privacy-data-protection"]
  }
}
