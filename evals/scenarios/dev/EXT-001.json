{
  "schema": "sef.eval.scenario.v1",
  "id": "EXT-001",
  "title": "Payment fulfillment webhook activates webhook trust governance",
  "set": "DEV",
  "layer": "L1",
  "family": "external-input",
  "severity": "critical",
  "fixture": "api-basic",
  "phase": "plan",
  "project_brief": "An API service that integrates with external providers.",
  "request": "Add a payment-provider webhook endpoint that triggers order fulfillment.",
  "reference_outcome": {
    "must_recognize": ["inbound webhook trust boundary", "privileged state mutation from external input"],
    "must_not_claim": ["payload can be trusted merely because it came to the webhook URL"]
  },
  "expect": {
    "risk": {"minimum": "R3"},
    "required_packs": ["WEBHOOK_TRUST"],
    "required_procedures": ["webhook-external-input-trust"]
  }
}
