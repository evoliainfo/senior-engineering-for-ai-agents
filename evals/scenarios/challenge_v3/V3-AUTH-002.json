{
  "schema": "sef.eval.scenario.v1",
  "id": "V3-AUTH-002",
  "title": "Department-scoped work items require isolation and authorization",
  "set": "CHALLENGE",
  "layer": "L1",
  "family": "auth-boundary",
  "severity": "critical",
  "fixture": "api-basic",
  "phase": "plan",
  "project_brief": "One operations application is shared by multiple internal departments with separate work queues.",
  "request": "An operator is assigned to one department and may edit work items only for that department. Passing another department's identifier must never expose or modify the other department's work items.",
  "reference_outcome": {
    "must_recognize": [
      "business partition isolation without tenant vocabulary",
      "authorization independent of caller-supplied department identifier"
    ]
  },
  "expect": {
    "risk": {
      "minimum": "R3"
    },
    "required_packs": [
      "MULTI_TENANT",
      "AUTHORIZATION"
    ],
    "required_procedures": [
      "multi-tenant-isolation",
      "security-authentication-authorization"
    ]
  }
}
