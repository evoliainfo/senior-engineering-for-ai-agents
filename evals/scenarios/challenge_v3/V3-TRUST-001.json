{
  "schema": "sef.eval.scenario.v1",
  "id": "V3-TRUST-001",
  "title": "Partner-controlled archive URI is a privileged server-side destination",
  "set": "CHALLENGE",
  "layer": "L1",
  "family": "external-input-trust",
  "severity": "critical",
  "fixture": "api-basic",
  "phase": "plan",
  "project_brief": "A backend ingestion worker can make outbound network connections from the private application network.",
  "request": "Partners submit an archive_uri with each import job. The backend worker retrieves the archive from that URI, opens it and extracts metadata before persisting the import.",
  "reference_outcome": {
    "must_recognize": [
      "lower-privilege actor controls a server-side network locator",
      "server-side destination trust boundary"
    ]
  },
  "expect": {
    "risk": {
      "minimum": "R3"
    },
    "required_packs": [
      "WEBHOOK_TRUST"
    ],
    "required_procedures": [
      "webhook-external-input-trust"
    ]
  }
}
