{
  "schema": "sef.eval.scenario.v1",
  "id": "V3-DATA-001",
  "title": "Live normalization of tens of millions of records needs migration, capacity and release controls",
  "set": "CHALLENGE",
  "layer": "L1",
  "family": "data-live-transformation",
  "severity": "critical",
  "fixture": "api-basic",
  "phase": "plan",
  "project_brief": "A production order service remains writable around the clock.",
  "request": "Normalize a legacy status field across 45 million stored order records while normal writes continue. Process the rewrite in bounded chunks, allow pause and resume, and keep database load and lock time under control during rollout.",
  "reference_outcome": {
    "must_recognize": [
      "large online data transformation",
      "capacity and locking pressure",
      "controlled rollout"
    ]
  },
  "expect": {
    "risk": {
      "minimum": "R3"
    },
    "required_packs": [
      "DATABASE_MIGRATION",
      "PERFORMANCE_CAPACITY_COST",
      "RELEASE_ENGINEERING"
    ]
  }
}
