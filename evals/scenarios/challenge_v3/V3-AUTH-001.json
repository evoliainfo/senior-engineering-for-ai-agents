{
  "schema": "sef.eval.scenario.v1",
  "id": "V3-AUTH-001",
  "title": "Attachment download requires object authorization even with a guessed identifier",
  "set": "CHALLENGE",
  "layer": "L1",
  "family": "auth-authorization",
  "severity": "critical",
  "fixture": "api-basic",
  "phase": "plan",
  "project_brief": "An authenticated customer-support API stores case notes and private customer attachments.",
  "request": "Add an attachment download endpoint. Support operators can open attachments only when they are authorized for the case; an unauthorized operator must not download an attachment even if they know its attachment identifier.",
  "reference_outcome": {
    "must_recognize": [
      "object-level authorization on attachment access"
    ],
    "must_not_claim": [
      "authentication or identifier secrecy is sufficient"
    ]
  },
  "expect": {
    "risk": {
      "minimum": "R3"
    },
    "required_packs": [
      "AUTHORIZATION"
    ],
    "required_procedures": [
      "security-authentication-authorization"
    ]
  }
}
